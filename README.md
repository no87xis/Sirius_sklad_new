# Система учёта склада «Сириус»

Веб-приложение для управления складом, заказами и поставками с ролевой системой доступа и аналитическими отчетами.

## 🚀 Возможности

### Основной функционал
- **Управление товарами**: добавление, редактирование, удаление товаров
- **Управление заказами**: создание, выдача, отмена заказов
- **Управление поставками**: регистрация поставок от поставщиков
- **Ролевая система**: администратор, менеджер, пользователь
- **Аналитика**: отчеты по продажам, остаткам, поставкам, прибыли
- **Админ-панель**: управление пользователями, просмотр логов

### Аналитические отчеты
- 📊 **Отчет по продажам**: динамика продаж, топ-товары
- 📦 **Отчет по остаткам**: текущие остатки, низкий запас
- 🚚 **Отчет по поставкам**: анализ поставщиков, стоимость поставок
- 💰 **Анализ прибыли**: рентабельность, маржинальность товаров
- 📈 **Экспорт данных**: CSV-файлы для дальнейшего анализа

### Безопасность
- 🔐 Аутентификация пользователей
- 👥 Ролевой доступ к функциям
- 📝 Логирование всех операций
- 🛡️ Защита от несанкционированного доступа

## 🛠️ Технологии

- **Backend**: FastAPI (Python 3.11+)
- **Database**: SQLAlchemy 2.x, SQLite
- **Authentication**: Starlette SessionMiddleware, bcrypt
- **Frontend**: Jinja2 templates, Tailwind CSS
- **Testing**: pytest, TestClient

## 📋 Требования

- Python 3.11+
- pip (менеджер пакетов Python)

## 🚀 Установка и запуск

### Быстрый минимальный запуск (без демо-данных)
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

pip install --upgrade pip wheel
pip install -r requirements.txt
python scripts/bootstrap_min_admin.py
uvicorn app.main:app --host 0.0.0.0 --port 8000
```
**Логин:** `admin` / `admin123`

### Полная установка с демо-данными

#### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd Sirius_sklad_new
```

#### 2. Создание виртуального окружения
```bash
python -m venv venv
```

#### 3. Активация виртуального окружения

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

#### 4. Установка зависимостей
```bash
pip install -r requirements.txt
```

#### 5. Создание базы данных и демо-данных
```bash
python scripts/create_demo_data.py
```

#### 6. Запуск приложения
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Приложение будет доступно по адресу: http://localhost:8000

## 👥 Пользователи по умолчанию

После создания демо-данных доступны следующие пользователи:

| Пользователь | Пароль | Роль | Доступ |
|-------------|--------|------|--------|
| `admin` | `admin123` | Администратор | Полный доступ ко всем функциям |
| `manager` | `manager123` | Менеджер | Управление товарами, заказами, аналитика |
| `operator` | `operator123` | Оператор | Создание заказов, просмотр товаров |
| `ivanov` | `ivanov123` | Пользователь | Базовые операции |
| `petrov` | `petrov123` | Менеджер | Управление товарами, заказами, аналитика |

## 📖 Руководство пользователя

### Вход в систему
1. Откройте http://localhost:8000
2. Нажмите "Войти в систему"
3. Введите логин и пароль
4. Нажмите "Войти"

### Управление товарами
1. В главном меню выберите "Товары"
2. Для добавления товара нажмите "Добавить товар"
3. Заполните форму и нажмите "Сохранить"
4. Для редактирования нажмите "Редактировать" рядом с товаром

### Создание заказа
1. В главном меню выберите "Заказы"
2. Нажмите "Новый заказ"
3. Заполните данные клиента и выберите товар
4. Нажмите "Создать заказ"

### Просмотр аналитики
1. Войдите как администратор или менеджер
2. В главном меню выберите "Аналитика"
3. Выберите нужный отчет
4. Используйте фильтры по датам при необходимости

### Админ-панель
1. Войдите как администратор
2. В главном меню выберите "Админ-панель"
3. Управляйте пользователями и просматривайте логи

## 🧪 Тестирование

### Запуск всех тестов
```bash
python -m pytest
```

### Запуск тестов с подробным выводом
```bash
python -m pytest -v
```

### Запуск конкретного теста
```bash
python -m pytest tests/test_basic_functionality.py::test_main_page -v
```

## 📁 Структура проекта

```
Sirius_sklad_new/
├── app/                          # Основной код приложения
│   ├── models/                   # Модели данных
│   ├── routers/                  # Маршруты (API и веб)
│   ├── services/                 # Бизнес-логика
│   ├── schemas/                  # Pydantic схемы
│   ├── templates/                # HTML шаблоны
│   ├── static/                   # Статические файлы
│   ├── db.py                     # Настройки базы данных
│   ├── deps.py                   # Зависимости FastAPI
│   └── main.py                   # Точка входа приложения
├── tests/                        # Тесты
│   ├── test_basic_functionality.py
│   ├── test_products_functionality.py
│   ├── test_orders_functionality.py
│   ├── test_analytics_functionality.py
│   └── test_admin_functionality.py
├── scripts/                      # Скрипты
│   ├── create_demo_data.py       # Создание демо-данных
│   └── bootstrap_min_admin.py    # Минимальная инициализация
├── requirements.txt              # Зависимости Python
├── README.md                     # Документация
└── .gitignore                    # Исключения Git
```

## 🔧 API Endpoints

### Аутентификация
- `POST /login` - Вход в систему
- `POST /logout` - Выход из системы
- `GET /register` - Страница регистрации

### Товары
- `GET /products` - Список товаров
- `POST /products` - Создание товара
- `GET /products/{id}` - Детали товара
- `POST /products/{id}` - Обновление товара
- `POST /products/{id}/delete` - Удаление товара

### Заказы
- `GET /orders` - Список заказов
- `POST /orders` - Создание заказа
- `GET /orders/{id}` - Детали заказа
- `POST /orders/{id}/issue` - Выдача заказа
- `POST /orders/{id}/deny` - Отказ в выдаче

### Поставки
- `GET /supplies` - Список поставок
- `POST /supplies` - Создание поставки
- `GET /supplies/{id}` - Детали поставки

### Аналитика
- `GET /admin/analytics` - Главная страница аналитики
- `GET /admin/analytics/sales` - Отчет по продажам
- `GET /admin/analytics/inventory` - Отчет по остаткам
- `GET /admin/analytics/supplies` - Отчет по поставкам
- `GET /admin/analytics/profit` - Анализ прибыли
- `GET /admin/analytics/export/sales` - Экспорт продаж в CSV
- `GET /admin/analytics/export/inventory` - Экспорт остатков в CSV

### Админ-панель
- `GET /admin` - Главный дашборд
- `GET /admin/users` - Список пользователей
- `POST /admin/users` - Создание пользователя
- `GET /admin/users/{username}` - Детали пользователя
- `POST /admin/users/{username}` - Обновление пользователя
- `POST /admin/users/{username}/delete` - Удаление пользователя
- `GET /admin/logs` - Логи операций

## 🐛 Устранение неполадок

### Проблема: "Module not found"
**Решение**: Убедитесь, что виртуальное окружение активировано и зависимости установлены:
```bash
pip install -r requirements.txt
```

### Проблема: "Database error"
**Решение**: Удалите файл базы данных и создайте заново:
```bash
rm sirius.db
python scripts/create_demo_data.py
```

### Проблема: "Port already in use"
**Решение**: Измените порт при запуске:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
```

### Проблема: "bcrypt version error"
**Решение**: Установлена совместимая версия bcrypt==3.2.2. Если проблема сохраняется:
```bash
pip uninstall bcrypt
pip install bcrypt==3.2.2
```

### Проблема: "Decimal * float error"
**Решение**: Все денежные вычисления переведены на Decimal. Перезапустите скрипт:
```bash
python scripts/create_demo_data.py
```

## 📝 Лицензия

Этот проект создан в учебных целях.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории проекта.
