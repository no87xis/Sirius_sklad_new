#!/usr/bin/env python3
"""
Тест API корзины
"""

import sys
import os
sys.path.append(os.path.dirname(os.path.abspath(__file__)))

def test_imports():
    """Тестируем импорты"""
    try:
        from app.routers.shop_api import router
        print("✅ shop_api router импортируется корректно")
    except ImportError as e:
        print(f"❌ Ошибка импорта shop_api router: {e}")
        return False
    
    try:
        from app.services.shop_cart import ShopCartService
        print("✅ ShopCartService импортируется корректно")
    except ImportError as e:
        print(f"❌ Ошибка импорта ShopCartService: {e}")
        return False
    
    try:
        from app.schemas.shop_cart import ShopCartCreate
        print("✅ ShopCartCreate схема импортируется корректно")
    except ImportError as e:
        print(f"❌ Ошибка импорта ShopCartCreate: {e}")
        return False
    
    return True

def test_router_routes():
    """Проверяем маршруты роутера"""
    try:
        from app.routers.shop_api import router
        
        # Проверяем, что роутер имеет правильный префикс
        if router.prefix == "/api/shop":
            print("✅ Роутер имеет правильный префикс /api/shop")
        else:
            print(f"❌ Неправильный префикс роутера: {router.prefix}")
            return False
        
        # Проверяем, что есть нужные маршруты
        routes = [route.path for route in router.routes]
        if "/cart/add-form" in routes:
            print("✅ Маршрут /cart/add-form найден")
        else:
            print("❌ Маршрут /cart/add-form не найден")
            print(f"Доступные маршруты: {routes}")
            return False
        
        return True
    except Exception as e:
        print(f"❌ Ошибка при проверке маршрутов: {e}")
        return False

def test_main_app():
    """Тестируем main.py"""
    try:
        from app.main import app
        print("✅ main.py импортируется корректно")
        
        # Проверяем, что роутер подключен
        routes = [route.path for route in app.routes]
        if "/api/shop/cart/add-form" in routes:
            print("✅ Маршрут /api/shop/cart/add-form подключен в main.py")
        else:
            print("❌ Маршрут /api/shop/cart/add-form не подключен в main.py")
            print(f"Доступные маршруты: {routes}")
            return False
        
        return True
    except ImportError as e:
        print(f"❌ Ошибка импорта main.py: {e}")
        return False

def main():
    """Основная функция тестирования"""
    print("🧪 Тестирование API корзины")
    print("=" * 50)
    
    tests = [
        ("Импорты", test_imports),
        ("Маршруты роутера", test_router_routes),
        ("Главное приложение", test_main_app)
    ]
    
    passed = 0
    total = len(tests)
    
    for test_name, test_func in tests:
        print(f"\n📋 {test_name}:")
        if test_func():
            passed += 1
        else:
            print(f"❌ Тест {test_name} провален")
    
    print("\n" + "=" * 50)
    print(f"📊 Результаты: {passed}/{total} тестов пройдено")
    
    if passed == total:
        print("🎉 API корзины настроен корректно!")
        print("\n💡 Если кнопка все еще не работает:")
        print("  1. Убедитесь, что сервер запущен: scripts\\win\\serve_dev.cmd")
        print("  2. Проверьте консоль браузера на ошибки JavaScript")
        print("  3. Проверьте Network tab в DevTools браузера")
        return True
    else:
        print("⚠️  Есть проблемы с API корзины.")
        return False

if __name__ == "__main__":
    success = main()
    sys.exit(0 if success else 1)

